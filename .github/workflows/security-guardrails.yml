name: Security Guardrails

on:
  pull_request:
    paths:
      - "app/**"
      - "supabase/migrations/**"
      - "scripts/go-live/**"
      - "package.json"
      - ".github/workflows/security-guardrails.yml"
  push:
    branches:
      - main
    paths:
      - "app/**"
      - "supabase/migrations/**"
      - "scripts/go-live/**"
      - "package.json"
      - ".github/workflows/security-guardrails.yml"

jobs:
  security-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate migration naming/order/version uniqueness
        run: npm run check:migrations

      - name: Validate migration security guardrails
        run: npm run check:security:migrations

      - name: Validate no service-role secret in client code
        run: npm run check:client-secrets

      - name: Validate secret patterns in tracked files
        run: npm run check:secret-patterns
